%1.
V = pointPotential(x,y,q);
V_1_1=pointPotential(1,1,1e-9)
dx=0.075; dy=0.075
[x,y]=meshgrid(-2:dx:2,-2:dy:2);
V=pointpotential(x,y,1e-9);
surf(x,y,V);


A=pointPotential(-0.5,1,1e-9)
plot(A)
%Do I plot it like the V? 

%2.
V=dipolePotential(x,y,q,d);
dx=0.075; dy=0.075
[x,y]=meshgrid(-2:dx:2,-2:dy:2);
V=dipolePotential(x,y,1e-9,d)
plot(V)





%3. 
x = -5:0.1:5;       %setting up an array
sigma = 2;          %assigning number to variable
y = myGauss(x,sigma); %function
plot(x,y)       
%help myGauss

%a)

